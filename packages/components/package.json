{
  "name": "@bitcoin-computer/components",
  "version": "1.0.0",
  "description": "",
  "main": "built/index.js",
  "types": "built/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc-compile": "rm -rf built/* && tsc",
    "install-monorepo": "cd ../../ && yarn install && cd packages/components",
    "compile": "yarn tsc-compile && yarn install-monorepo"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bitcoin-computer/lib": "^0.16.0-beta.1",
    "flowbite": "^2.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
