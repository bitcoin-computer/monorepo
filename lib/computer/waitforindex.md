# waitForIndex

!!!
This page name is outdated. The method is **`waitForIndexed`**. See [waitForIndexed](./waitForIndexed.md).
!!!

Redirect: use [`computer.waitForIndexed(location, opts?)`](./waitForIndexed.md).
